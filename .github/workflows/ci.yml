name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm run compile
      - run: npx vsce package --no-dependencies -o claude-code-usage.vsix
      - uses: actions/upload-artifact@v4
        with:
          name: vsix
          path: claude-code-usage.vsix
